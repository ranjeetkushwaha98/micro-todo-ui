apiVersion: v1
kind: Pod
metadata:
  name: testpod
  namespace: ranjeet
  labels:
    app: web
spec:
  containers:
  - name: test
    image: sohitacr.azurecr.io/todo-ui:IMAGE_VERSION
    ports:
    - containerPort: 80
